# Chaos

## 题目简述

题目提供一个 Windows 可执行文件。总 WP 对该题只给出“运行 exe 即可得到 flag”这一结论；赛事仓库中的题目目录也未归档附件、源码或更详细的复现材料，因此不能据此补写不存在的逆向过程。

## 解题过程

先在隔离的 Windows 环境中直接运行附件并记录标准输出或程序界面。该程序不要求构造输入，执行后会直接显示 flag，将输出中符合 `RCTF{...}` 格式的字符串提交即可。

如果本地执行没有输出，应先核对附件是否完整、系统位数及运行库，而不是凭空引入静态分析步骤。由于当前公开仓库的 `Reverse/Chaos` 目录仅有一份 TODO 说明，本文无法进一步验证 flag 常量的存放位置或程序内部实现。

## 方法总结

这是一道强调“先动态观察”的入门题：拿到可执行文件时，先在隔离环境运行并检查输出，再决定是否需要字符串搜索、调试或反编译。当前资料只足以确认直接执行这条解法，不能把未公开的内部机制写成既成事实。
