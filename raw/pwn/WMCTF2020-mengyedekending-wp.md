# mengyedekending

## 题目简述

本题是 C# 程序中的栈溢出。预期利用不是直接覆盖返回地址，而是覆盖下标变量，再借该下标间接修改核心标志量；标志量满足条件后会触发后门。题目没有充分处理连续 `\r`，因此可以形成可重复触发的栈溢出；同时 C# 字符串/输入涉及宽字节，payload 构造时要考虑字节布局和可控字符限制。

## 解题过程

使用了C#编写的栈溢出，预期解是覆盖掉下标变量进而间接修改掉核心标志量，当核心标志被修改即可触发后门，由于没有考虑过多`\r`的处理导致存在一个无限栈溢出，另外由于C#使用的是宽字节，因此输入也会造成一定的阻碍。

[EXP.zip](https://uploader.shimo.im/f/IFrJZ9u5ePax1AZi.zip)

上面的链接是官方材料中给出的 exploit 附件入口，当前 Markdown/PDF 文本没有展开附件脚本内容。就正文可确认的信息而言，附件应当用于演示如何通过 `\r` 触发重复栈溢出、覆盖下标变量并最终改写核心标志量；若后续要做完整复现，需要把附件中的具体 payload 结构、宽字节处理方式和触发交互补入本文。

P.S. 其实最一开始的出题思路是C#委托劫持，但是没有时间再去调了。

## 方法总结

C# pwn 题需要先判断可控数据最终落在原生栈、托管对象还是运行时封装的缓冲区中。本题可复用的思路是把栈溢出转成“下标变量污染”，再利用程序自身的数组/索引写逻辑修改关键标志位；连续 `\r` 带来的无限溢出提高了容错率，但宽字节输入会改变 payload 的实际内存布局，调试时必须按最终字节序列验证。
